# Solana RPC endpoint the dApp connects to (browser-exposed).
# Defaults to the public devnet RPC if unset. For production use a dedicated
# devnet endpoint (Helius, QuickNode, Alchemy, Triton) to avoid rate limits.
NEXT_PUBLIC_RPC_URL=https://api.devnet.solana.com
